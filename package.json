{
  "name": "vku-field-survey",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "build:apk": "npm run build && npx cap sync && cd android && .\\gradlew.bat assembleRelease && cd ..",
    "build:debug": "npm run build && npx cap sync && cd android && .\\gradlew.bat assembleDebug && cd .."
  },
  "dependencies": {
    "@capacitor/android": "^7.0.1",
    "@capacitor/camera": "^7.0.0",
    "@capacitor/cli": "^7.0.1",
    "@capacitor/core": "^7.0.1",
    "@capacitor/geolocation": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
